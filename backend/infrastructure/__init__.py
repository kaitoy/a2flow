"""Infrastructure layer: concrete adapters for external systems and persistence.

Holds technical implementations (session storage, git-backed skill cloning) that
the use case (service) layer depends on. This layer contains no business rules.
"""
